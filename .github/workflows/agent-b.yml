name: Agent B global bounds

on:
  push:
    branches:
      - agent-b-global-bounds
    paths-ignore:
      - agent-b-ci.log
  pull_request:
    branches:
      - proof/wowii-146
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bounds:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install elan
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Resolve dependencies
        run: lake update

      - name: Download Mathlib cache
        run: lake exe cache get

      - name: Check global bounds module
        id: bounds
        shell: bash
        run: |
          set +e
          {
            echo '$ lake build WOW146.GlobalBounds'
            lake build WOW146.GlobalBounds
            echo '$ lake env lean -DwarningAsError=true WOW146/GlobalBounds.lean'
            lake env lean -DwarningAsError=true WOW146/GlobalBounds.lean
          } > agent-b-ci.log 2>&1
          status=$?
          printf '\nEXIT_STATUS=%s\n' "$status" >> agent-b-ci.log
          cat agent-b-ci.log
          echo "exit_status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agent-b-ci-${{ github.run_id }}
          path: agent-b-ci.log

      - name: Publish diagnostics on branch pushes
        if: github.event_name == 'push'
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -f agent-b-ci.log
          git commit -m "ci: publish Agent B diagnostics" || true
          git push origin HEAD:${GITHUB_REF_NAME}

      - name: Fail job when Lean failed
        if: steps.bounds.outputs.exit_status != '0'
        run: exit 1
